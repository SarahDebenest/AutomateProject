{
    "liste": [
        {% for patient in liste %}
        {
            "id": "{{patient.idPatient  }}",
            "nom" : "{{patient.nomPatient  }}"
        }
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}